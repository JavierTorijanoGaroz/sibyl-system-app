import { EquipamentFilterPipe } from './equipament-filter.pipe';

describe('EquipamentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipamentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
